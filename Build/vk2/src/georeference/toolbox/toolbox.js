goog.provide('vk2.georeference.toolbox.Toolbox');

goog.require('goog.events.EventTarget');

/**
 * @constructor
 * @extends {goog.events.EventTarget}
 */
vk2.georeference.toolbox.Toolbox = function(){
	
	goog.base(this);
};
goog.inherits(vk2.georeference.toolbox.Toolbox, goog.events.EventTarget);